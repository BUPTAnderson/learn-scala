package learn15

/**
 * Created by kongyunlong on 2017/9/2.
 */
object CaseDemo04 {
  def test (name : String, age : Int): Unit = {
    val sql = s"select * from student where name='${name}' and age >= ${age}"
    val s =
    """
      select * from
      student
      where
    """
  }
}
